---
layout: post
title: "FAHNES: Feature-Aware Hypergraph Generation via Next-Scale Prediction"
date: 2025-06-02
tags: machine-learning generative-models graph-theory hypergraphs
categories: papers
---

This blog post summarizes our paper, [**FAHNES: Feature-Aware Hypergraph Generation via Next-Scale Prediction**](). It expands our work [**HYGENE: A Diffusion-based Hypergraph Generation Method**](https://doriangailhard.github.io/blog/2024/HYGENE/).

**TL;DR:** We introduce a framework able to generate hypergraphs with node and hyperedge features, based on an "unzoom-zoom" principle. First, we compute increasingly coarse versions of a hypergraph ("unzoom"), keeping track of the number of nodes in each supernode/superhyperedge and averaging features. Then, we train a model to reconstruct the original details from these simplified versions ("zoom"). Generation is achieved by repeatedly applying the trained model to zoom in, gradually increasing detail until the desired hypergraph size is reached.


## Preliminaries


Our work aims to develop a deep-learning based method capable of generating new samples from a given distribution of featured hypergraphs. For instance, given examples of 3D meshes representing variants of an object, we want to train a model that can generate new instances. Generating topology was already addressed with [**HYGENE: A Diffusion-based Hypergraph Generation Method**](https://doriangailhard.github.io/blog/2024/HYGENE/), so here we focus on improving the quality of generated topology and adding support for features.

<div class="row justify-content-sm-center">
    <div class="col-sm mt-3 mt-md-0">
        {% include figure.liquid loading="eager" path="assets/img/fahnes/train_airplane.png" class="img-fluid rounded z-depth-1" zoomable=true %}
    </div>
    <div class="col-sm mt-3 mt-md-0">
        {% include figure.liquid loading="eager" path="assets/img/fahnes/train_bench.png" class="img-fluid rounded z-depth-1" zoomable=true %}
    </div>
</div>
<div class="caption">
    Examples of datasets we tested our method against
</div>

In other words, given a dataset of featured hypergraphs (with 3D positions, component types, or other characteristics) that share similar topological properties, our goal is to train a model capable of generating new hypergraphs with matching properties.


## Main inspiration

### FlowAR: Scale-wise Autoregressive Image Generation Meets Flow Matching


### HYGENE: A Diffusion-based Hypergraph Generation Method

Our method for hypergraph generation builds on our previous work [**HYGENE: A Diffusion-based Hypergraph Generation Method**](https://doriangailhard.github.io/blog/2024/HYGENE/), which uses an "unzoom–zoom" scheme:

---

### 1. Hypergraph Downsampling (Unzoom)

To enable coarsening on hypergraphs, we project them into graph space using two complementary representations:

- **Clique Expansion**: Each hyperedge is replaced with a clique of edges weighted by \(1 / |e|\), where \(|e|\) is the hyperedge size. This preserves spectral properties, allowing us to apply traditional coarsening methods.

- **Star Expansion (Bipartite View)**: The hypergraph is represented as a bipartite graph. One set of nodes corresponds to original hypergraph nodes, the other to hyperedges, with links connecting nodes to the hyperedges they belong to.

We apply a **spectrum-preserving coarsening** algorithm to the clique expansion to produce a hierarchy of coarsened graphs. The same node merges are mirrored in the bipartite representation, and hyperedges are merged when their node sets become identical (i.e. the same hyperedge appears twice).

<div class="row justify-content-sm-center">
    <div class="col-sm mt-3 mt-md-0">
        {% include figure.liquid loading="eager" path="assets/img/hygene/clique_expansion.png" class="img-fluid rounded z-depth-1" zoomable=true %}
    </div>
    <div class="col-sm mt-3 mt-md-0">
        {% include figure.liquid loading="eager" path="assets/img/hygene/clique_coarsening.png" class="img-fluid rounded z-depth-1" zoomable=true %}
    </div>
    <div class="col-sm mt-3 mt-md-0">
        {% include figure.liquid loading="eager" path="assets/img/hygene/bipartite_coarsening.png" class="img-fluid rounded z-depth-1" zoomable=true %}
    </div>
</div>
<div class="caption">
    Coarsening: <b>(a)</b> Compute the weighted clique expansion by collapsing each hyperedge into an appropriately weighted clique. <b>(b)</b> Coarsen the clique expansion while preserving the spectral properties of the hypergraph (dark blue nodes). <b>(c)</b> Update the bipartite view: corresponding left side nodes (in dark blue) are merged, then right side nodes representing the same hyperedge (circled in black) are merged.
</div>

---

### 2. Hypergraph Upsampling (Zoom)

From a reduced bipartite graph, we train a neural network to reverse the coarsening process:

- **Expansion**: Predicts which nodes and hyperedges should be duplicated at each step. New entities inherit connections from their parent nodes/hyperedges.

- **Refinement**: Filters the resulting edges to ensure structural similarity.

For generation, starting with a single node and single hyperedge, we apply this expansion + refinement step iteratively to recover a full-size hypergraph.

<div class="row justify-content-sm-center">
    <div class="col-sm mt-3 mt-md-0">
        {% include figure.liquid loading="eager" path="assets/img/hygene/hypergraph_generation.png" class="img-fluid rounded z-depth-1" zoomable=true %}
    </div>
</div>
<div class="caption">
    Our model starts from a single pair of linked nodes (in the bipartite representation) and iteratively expands the left-side nodes (in dark blue) and right-side nodes (in red), where each duplicate keeps the connections of its parent node. Then, our method refines the resulting bipartite graph filtering edges to recover an appropriate local structure.
</div>


## Our Work

We extend the "zoom-unzoom" strategy of **HYGENE** by adding two components:
- A **node-budget mechanism**, representing the size of each cluster (i.e. how many nodes it contains). During coarsening, every node and hyperedge starts with a node budget of one (coarsening has not started yet, so each "cluster" represents a single node, itself) ; then, for each merging of clusters, the resulting supernode / superhyperedge inherits the sum of the node budgets of the nodes / hyperedges it contains. During generation, the model is tasked with predicting how the node budget of a cluster is split between its children. That way, at every step of the generation, the model knows how many more nodes each "region" of the hypergraph is going to obtain, even when the hypergraph is too large to fit in the receptive field of the model.
- **Node/hyperedge features**, which allow to add local properties to the hypergraph. During coarsening, these features are averaged when merging several nodes, the weights in the averaging being the node budgets. That way, each cluster has as feature the average of all the nodes it contains. During generation, children of a node / hyperegde obtain the same feature as their parent ("coarse features"), and the model is tasked with predicting the new features ("refined features").


### Coarsening Sequence / Downsampling

<div class="row justify-content-sm-center">
    <div class="col-sm mt-3 mt-md-0">
        {% include figure.liquid loading="eager" path="assets/img/fahnes/coarsening_seq_example1.png" class="img-fluid rounded z-depth-1" zoomable=true %}
    </div>
    <div class="col-sm mt-3 mt-md-0">
        {% include figure.liquid loading="eager" path="assets/img/fahnes/coarsening_seq_example2.png" class="img-fluid rounded z-depth-1" zoomable=true %}
    </div>
    <div class="col-sm mt-3 mt-md-0">
        {% include figure.liquid loading="eager" path="assets/img/fahnes/coarsening_seq_example3.png" class="img-fluid rounded z-depth-1" zoomable=true %}
    </div>
    <div class="col-sm mt-3 mt-md-0">
        {% include figure.liquid loading="eager" path="assets/img/fahnes/coarsening_seq_example4.png" class="img-fluid rounded z-depth-1" zoomable=true %}
    </div>
</div>
<div class="caption">
    Examples of coarsening sequences for various meshes.
</div>

### Coarsening Inversion / Upsampling

During inversion of the coarsening, in order to have a constrained space for the predictions of node budgets, the model is not tasked to directly generate the resulting node budgets for all children of a cluster, but rather tasked with predicting the **splitting** of node budgets. Indeed, during training, we task it to predict the ratio between the ground truth value for the refined node budget and the parent node budget. That way, predictions are constrained in [0, 1], which helps learning as it reduces the variance of target outputs. Additionally, to ensure that input variance is also controlled, we use a sinusoidal positional encoding to transform the integers.

For features, the process is done similarly as for hierarchical image generation. First, the children nodes of a cluster are first given the same feature as their parent (i.e. we have a "pixelated" version of the hypergraph). Then, based on these coarse features, the model enhances and refines the features (i.e. it computes the clear hypergraph from its pixelated version).



### Pipeline

<div class="row justify-content-sm-center">
    <div class="col-sm mt-3 mt-md-0">
        {% include figure.liquid loading="eager" path="assets/img/hygene/pipeline.png" class="img-fluid rounded z-depth-1" zoomable=true %}
    </div>
</div>
<div class="caption">
    Combining everything gives the above pipeline: <b>(a)</b> . <b>(b)</b> . <b>(c)</b> .
</div>

We also leverage prior knowledge given by the node budgets during generation, replacing the predictions of the model by ground truth values using inpainting:
- nodes having a budget of one cannot be expanded
- expanded clusters with a budget of two necessarily gives half of their budget to each child node
- unexpanded clusters keep the same budget and same features


## Results and Limitations

**Results**

<div class="row justify-content-sm-center">
    <div class="col-sm mt-3 mt-md-0">
        {% include figure.liquid loading="eager" path="assets/img/fahnes/generated_airplane.png" class="img-fluid rounded z-depth-1" zoomable=true %}
    </div>
    <div class="col-sm mt-3 mt-md-0">
        {% include figure.liquid loading="eager" path="assets/img/fahnes/generated_bench.png" class="img-fluid rounded z-depth-1" zoomable=true %}
    </div>
    <div class="col-sm mt-3 mt-md-0">
        {% include figure.liquid loading="eager" path="assets/img/fahnes/generated_qm9.png" class="img-fluid rounded z-depth-1" zoomable=true %}
    </div>
</div>
<div class="caption">
    Examples of generated samples corresponding to the datasets shown at the beginning.
</div>


**Limitations**

Results for molecule generate show that our method is not competitive compared to one-shot graph prediction methods, unlike hierarchical image generation which is stronger than regular one-shot methods. We hypothesize that the difference stems from the discrete nature of the generation process: for continuous predictions with fixed topology (like for images), the "latent space" is "continuous", in the sense that nodes (i.e. pixels) of slightly different images have slightly different representations. Thus a small error during a prediction step results in a slightly different starting point. This is not true in our discrete setting, due to the tresholding effect of either adding an edge or removing it, expanding a cluster or not: a small error at a prediction step results in a different topology, which changes a lot the underlying embedding (with a GNN, either the information passes or not). One-shot models do not suffer from that, as the topology (a fully connected graph) is fixed during generation.


## Conclusion

This work represents a significant first step towards featured hierarchical (hyper)graph generation. Our future research will focus on rendering the framework more robust to perturbations / prediction errors.

When using our work, please cite our paper:

```bibtex
@inproceedings{gailhard2025hygene,
  title={Hygene: A diffusion-based hypergraph generation method},
  author={Gailhard, Dorian and Tartaglione, Enzo and Naviner, Lirida and Giraldo, Jhony H.},
  booktitle={Proceedings of the AAAI Conference on Artificial Intelligence},
  volume={39},
  number={16},
  pages={16682--16690},
  year={2025}
}
```



**Acknowledgments**

We acknowledge the [ANR – FRANCE](https://anr.fr/) (French National Research Agency) for its financial support of the [System On Chip Design leveraging Artificial Intelligence (SODA)](https://anr.fr/Project-ANR-23-IAS3-0004) project under grant ANR-23-IAS3-0004. This project has also been partially funded by the [Hi!PARIS Center on Data Analytics and Artificial Intelligence](https://www.hi-paris.fr/).
